# このファイルを .env にコピーして、公開するドメインを設定する。
# DNS の A レコードをこのサーバの IP へ向けておくこと。
# この値は ntfy の base-url になり、添付の絶対 URL にも使われる。前段のリバースプロキシが
# serve するホスト名と一致させること（README.md「前段のリバースプロキシへの要件」）。
PERANTA_DOMAIN=ntfy.example.com
